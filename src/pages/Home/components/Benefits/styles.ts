import { styled } from "styled-components";

export const Benefits = styled.div`
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 3rem;

    & > div{
        display: flex;
        align-items: center;
        gap: 0.75rem;
    }
`
